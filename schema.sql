CREATE TABLE fall_2019_ranking (
	Rank INT NOT NULL,
	Player VARCHAR NOT NULL,
	Points INT NOT NULL,
	Country VARCHAR,
	Characters VARCHAR NOT NULL,
	PRIMARY KEY (Player)
);

CREATE TABLE fall_2021_ranking (
	Rank INT NOT NULL,
	Player VARCHAR NOT NULL,
	Points INT NOT NULL,
	Country VARCHAR,
	Characters VARCHAR NOT NULL,
	PRIMARY KEY (Player)
);

CREATE TABLE fall_2022_ranking (
	Rank INT NOT NULL,
	Player VARCHAR NOT NULL,
	Points INT NOT NULL,
	Country VARCHAR,
	Characters VARCHAR NOT NULL,
	PRIMARY KEY (Player)
);

CREATE TABLE spring_2019_ranking (
	Rank INT NOT NULL,
	Player VARCHAR NOT NULL,
	Points INT NOT NULL,
	Country VARCHAR,
	Characters VARCHAR NOT NULL,
	PRIMARY KEY (Player)
);

CREATE TABLE spring_2020_ranking (
	Rank INT NOT NULL,
	Player VARCHAR NOT NULL,
	Points INT NOT NULL,
	Country VARCHAR,
	Characters VARCHAR NOT NULL,
	PRIMARY KEY (Player)
);

CREATE TABLE spring_2022_ranking (
	Rank INT NOT NULL,
	Player VARCHAR NOT NULL,
	Points INT NOT NULL,
	Country VARCHAR,
	Characters VARCHAR NOT NULL,
	PRIMARY KEY (Player)
);

CREATE TABLE spring_2023_ranking (
	Rank INT NOT NULL,
	Player VARCHAR NOT NULL,
	Points INT NOT NULL,
	Country VARCHAR,
	Characters VARCHAR NOT NULL,
	PRIMARY KEY (Player)
);